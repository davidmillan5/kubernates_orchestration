apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-a-deployment
spec:
  selector:
    matchLabels:
      app: myapp-a
  template:
    metadata:
      labels:
        app: myapp-a
    spec:
      containers:
      - name: myapp-a
        image: dvago88/service_a:latest
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 9090
